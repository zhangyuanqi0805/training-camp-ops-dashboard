# 602期数据质量问题清单

## P0：第一版看板必须显式处理

- 正式班级配额合计为 `1022`，正式 14 班明细人数为 `1021`；两者相差 `1`。
  - `602期63班` 配额 `80`，明细 `79`，差异 `1`。
- 后台名单中存在非正式 14 班记录 `82` 条，第一版默认不纳入正式经营盘。
  - `602期101班`：79 条。
  - `602期171班`：2 条。
  - `602期55班`：1 条。
- 进阶表支付完成总额 `346300`，正式 14 班营收 `333800`；非正式班级金额 `12500` 需单列。
  - `测试102班`：3 单，金额 4470。
  - `601期179班`：1 单，金额 1490。
  - `(空白)`：1 单，金额 6540。

## P1：需要二次清洗后再进入看板

- `200订金订单` sheet 的 Excel 使用区域被记录为百万行级，不能按 `max_row` 直接全量读取；需要识别真实非空区域。
- 三场直播明细中存在原始班级为 `#N/A` 或空的记录；本快照已尝试用 `unionId` 桥接回填。
  - 原始班级缺失/异常 `347` 条；已回填 `88` 条；仍未回填 `259` 条。
- D1/D2 日报表中带班学员配额为 1022，D3-D10 多处为 1021；趋势看板必须保留每日分母字段。
- 测评字段的完成取值需要确认，不能简单用非空判断完成。

## P2：暂不进入第一版主看板

- ROI、CAC 缺少投放成本表，不得填具体值。
- `绩效表` 包含提成、税前绩效、身份证号、银行账号等强敏感字段，仅限内部口径核验。
- `退费` sheet 当前只有表头；当前营收是支付完成金额，不是扣退费后的净收入。
- 6027 人上游原始流量池未在当前 6 个 Excel 中看到完整行级表，不能作为看板分母。

## 已采取的保护

- 结构化 CSV 不写明文手机号、uid、unionid、订单号，只保留哈希键用于内部匹配。
- 直播标签不展开原文，只保留标签数量。
- 默认看板口径只使用正式 14 班；非正式班级单独标记。

